# 5라운드 플레이 피드백 3건 (2026-08-01)

사용자가 `4560605` 빌드를 직접 플레이하고 지적.

---

## ① 해설이 화면보다 먼저 나온다 — 최우선

> 해설이랑 화면이랑 안맞아!! 해설이 먼저 나오고 화면이 다음에 나와.
> **"김승규가 슛을 막았습니다"가 나오고 그에 맞는 화면이 다음에 나와. 무슨 예지력이야?**
> 화면을 보고 해설하게 해야지!!!

### 구조상 원인 (추정 — 확인 필요)

재생은 분 단위다. 한 분에 들어오면:
- `MatchScreen`이 그 분의 발화를 **분 시작 시점**에 큐에 넣는다
- 안무는 dwell(골 8.6초 등) 전체에 걸쳐 재생되고, **임팩트는 그 중간**이다

그래서 "막았습니다"가 GK 접촉보다 수 초 먼저 나온다.

### 처방 방향

발화를 **장면의 해당 순간**에 맞춰라. `f41266c`·`74b9330`이 이미 임팩트·접촉 시각을
프레임 단위로 알고 있다(`GK 접촉`, `킥 임팩트`). 캐스터 라인은 **결과가 화면에 보인 직후**
나와야 하고, 해설위원 라인은 그보다 더 뒤다.

**주의**: dwell은 발화 길이를 하한으로 쓴다(`minuteDwellWithSpeech`). 발화를 뒤로 밀면
그만큼 dwell이 늘어야 잘리지 않는다. 총 경기 시간이 늘어나는 것은 허용된다.

---

## ② 오프사이드가 없다

> 2D, 3D 전부 다 **오프사이드에서 패스를 받고 슛을 하고 있어.**
> 오프사이드면 골이 취소되어야지. **골이 인정되는거면 오프사이드가 아니게 슛을 해야지!!!!**

사용자 캡처(체코 공격): 파트리크 시크(파랑 10)가 페널티 박스 안에서 공을 받는데,
한국 최종 수비(빨강 2·4)는 **하프라인 근처**에 있다. GK만 뒤에 있다 — 명백한 오프사이드다.

### 두 층위

1. **저술 좌표가 오프사이드다** (필수) — `scenes.ts`의 마무리 배역이 최종 수비 뒤에 선다.
   패스 시점에 **뒤에서 두 번째 수비수**보다 앞서면 안 된다.
2. **엔진에 오프사이드 이벤트가 없다** (선택) — 취소된 골은 드라마이지만 엔진·밸런스
   게이트를 건드린다. 판단해서 결정하라.

사용자가 둘 중 하나면 된다고 명시했다. **1번이 최소 요구**다.

---

## ③ 공격 패턴이 하나뿐이다

> 시뮬레이션에서 선수들의 패턴이 모두 똑같아. **항상 중앙에서 공격수한테 패스하고
> 공격수가 슛을 쏘고 있어.** 사이드에서 크로스 올리고 헤딩을 할수도 있는거고,
> 드리블 돌파를 할수도 있는거고 여러가지 공격패턴이 있는데 하나만 보이고 있어!!!
> **최소 5개의 공격 패턴**을 만들어서 사용하게 해!!!

### 원인 (확정)

커밋 `c701946`이 **`attackPattern`이 빌드업 계열을 팀당 1종으로 고정**하도록 설계했다.
그때는 "유저 전술이 화면에 보인다"는 가치를 위한 의도였고, 실제로 그 목적은 달성됐다.

**부작용이 이것이다.** 유저가 기본(`balanced`)을 두면 90분 내내 `central` 하나만 나온다.

### 처방 방향

`attackPattern`은 **분포를 기울이는 것**이지 고정하는 것이 아니어야 한다.
예: `cross`를 고르면 측면 60% / 중앙 20% / 침투 20%. 기본은 고르게.

그러면 **전술이 화면에 보인다는 가치도 유지된다** — 비율이 눈에 띄게 다르면 된다.

### 최소 5개 패턴

사용자가 예로 든 것: **측면 크로스 → 헤딩**, **드리블 돌파**. 그 외 실제 축구의 득점 루트:
컷백, 원투 패스, 스루패스 뒷공간, 중거리, 세트피스 헤더, 역습 속공.

현재 빌드업 4계열(central / wing / through / outside) × 마무리 3종(goal/save/miss)이
있으나 **마무리 동작이 전부 발로 차는 슛**이다. **헤딩·드리블 돌파가 없다.**

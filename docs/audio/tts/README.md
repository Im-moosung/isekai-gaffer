# Gemini TTS 전량 생성 — 무엇을 몇 번의 요청으로 굽는가

프로토타입 판정(`docs/audio/tts-proto/README.md`)에서 **B안(캐리어 절단) 확정**을 받은
뒤의 실행 계획이다. 여기 있는 숫자는 전부 **코드를 돌려서 잰 값**이다 — 추측이 없다.

| | |
|---|---|
| 캐스터 | **Puck** / `gemini-2.5-flash-preview-tts` |
| 해설위원 | **Iapetus** / `gemini-3.1-flash-tts-preview` |
| 병목 | **모델당 RPD 10** (TPM 10K는 실측 92토큰이라 남아돈다) |
| 후처리 | `atempo=1.15` · `loudnorm=I=-16:TP=-1.5:LRA=11` · mp3 64k |
| 런타임 | **호출하지 않는다.** 빌드 타임에 구워 `public/tts/`에 커밋한다 |

> 런타임 호출을 금지하는 이유는 약관이다. 배포물이 API Client가 되면 연령·지역 제한이
> 걸린다. 생성물 자체는 소유권 주장이 없고 preview 모델도 프로덕션 사용이 허용된다.

## ① 이름 범위 — 312명이 아니라 147명이다

`tools/tts/roster.mjs`가 캠페인 24시드를 실제로 시뮬레이션해서(입장 소개 + 경기 중계 +
교체) **이름이 발화되는 선수**를 셌다.

| 팀 | 스쿼드 | 발화됨 | 클립 대상 |
|---|---:|---:|---:|
| 대한민국 | 26 | 16 | **26** (유저가 라인업을 바꾸므로 전원이 후보) |
| 나머지 11팀 | 각 26 | 각 11 | 각 **11** (AI는 `pickBestXI`가 결정론적으로 XI를 고정한다) |
| 합계 | 312 | | **147** |

- AI 팀은 24시드 전체에서 **선발 XI 11명을 넘어서지 않았다**. 교체로 들어온 선수도
  이름이 불린 적이 없다(엔진의 AI 교체가 이름 템플릿을 타지 않는다).
- 한국만 예외다. 출장정지·유저 조작으로 XI가 바뀌므로 26명 전원을 후보로 잡는다.
  (24시드 자동 진행만으로도 16명이 실제로 불렸다.)

### 화자 분포 — 해설위원 이름 클립은 **0개로 만들 수 있었다**

| 화자 | 이름이 든 문장 | 비율 |
|---|---:|---:|
| 캐스터 | 3,365 | 95.9% |
| 해설위원 | 145 | 4.1% |

해설위원이 이름을 부르는 템플릿은 **단 하나**(`an.save.1`)였고, 그 이름은 언제나
**선방한 골키퍼**였다. 그래서 이름을 역할어로 바꿨다 — 뜻이 그대로다.

```
- {선수}, 자리 선정이 좋았습니다.
+ 골키퍼, 자리 선정이 좋았습니다.
```

해설위원 모델의 RPD 10이 통째로 남는다. **적용했다**(`src/game/commentary.ts`).

## ② 조사 역할 — 실제 병목은 조사가 아니라 예산이었다

프로토 README의 권고는 "TSV의 조사 7종(`∅·의·이/가·은/는·을/를·에게·입니다`)을
`∅ + 이/가` 2종으로 줄여 312×7을 312×2로"였다. 재작성안은 아래에 남긴다. 다만
**지금은 적용하지 않았다** — 판단 근거는 §③이다. 경기 중 중계는 이번 마감 안에
mp3로 구울 수 없어서, 지금 20문장을 고치면 *돌아오는 것 없이* 출시 중인 중계 문장만
흔든다(문장 감사 baseline·동시 진행 중인 밸런스 갈래).

재작성안(소리 내어 읽어 확인한 것):

| id | 지금 (조사) | 재작성 (∅ 또는 이/가) |
|---|---|---|
| `foul.1` | {P}**의** 반칙으로 흐름이 끊깁니다. | {P}**,** 반칙입니다. 흐름이 끊깁니다. |
| `foul.2` | {P}**는** 늦게 들어갔습니다. | {P}**,** 늦게 들어갔습니다. |
| `miss.1` | {P}**의** 슛, 골문을 벗어납니다. | {P}**,** 슛이 골문을 벗어납니다. |
| `miss.5` | {P}**는** 고개를 떨굽니다. | {P}**,** 고개를 떨굽니다. |
| `shot.2` | {P}**의** 슈팅, 골문을 향합니다! | {P}**,** 슈팅이 골문을 향합니다! |
| `shot.4` | {P}**는** 과감하게 노려봅니다. | {P}**,** 과감하게 노려봅니다. |
| `shot.7` | {P}**의** 발끝을 떠납니다! | {P}**,** 발끝을 떠납니다! |
| `chance.1` | {P}**는** 여기서 공간을 찾아냅니다. | {P}**,** 여기서 공간을 찾아냅니다. |
| `chance.3` | {P}**에게** 찬스! | {P}**,** 찬스! |
| `save.2` | {P}**의** 세이브! | {P}**,** 세이브! |
| `yellow.1` | 주심이 {P}**에게** 경고를 줍니다. | {P}**,** 주심이 경고를 줍니다. |
| `yellow.2` | 경고 하나가 나옵니다. {P}**입니다**. | {P}**,** 여기서 경고를 받습니다. |
| `yellow.3` | {P}**는** 경고를 피하지 못합니다. | {P}**,** 경고를 피하지 못합니다. |
| `red.1` | 주심이 {P}**를** 그라운드 밖으로 내보냅니다. | {P}**,** 여기서 그라운드를 떠납니다. |
| `streak.fouls.1` | 또 {P}**입니다**. 반칙이 잦네요. | {P}**,** 또 걸립니다. 반칙이 잦네요. |
| `goal.equalizer.1` | 따라붙습니다! {S}! {P}**의** 골! | 따라붙습니다! {S}! {P}**,** 골입니다! |
| `goal.extra.1` | {T}, 한 골 더 달아납니다! {P}**의** 골! | {T}, 한 골 더 달아납니다! {P}**,** 골입니다! |
| `goal.clincher.2` | {P}**의** 골. 이걸로 사실상 승부는 결정됐습니다. | {P}**,** 골입니다. 이걸로 사실상 승부는 결정됐습니다. |
| `goal.chase.2` | {S}. {P}**의** 골로 간격을 좁힙니다. | {S}. {P}**,** 골입니다. 간격을 좁힙니다. |
| `goal.consolation.2` | 영패는 면했습니다. {P}**의** 골입니다. | 영패는 면했습니다. {P}**,** 골입니다. |
| `goal.lateDrama.2` | 다 끝난 경기를 되돌립니다! {P}**의** 극장골! | 다 끝난 경기를 되돌립니다! {P}**,** 극장골! |

나머지 이름 템플릿(`{P}!`, `{P}, 선방!`, `{P}가 손끝으로 걷어냅니다!` …)은 이미
`∅`이거나 `이/가`다 — 손댈 것이 없다.

### 적용한 재작성 두 건 (입장 소개 — 이번 범위)

| 어디 | 전 | 후 | 왜 |
|---|---|---|---|
| 골키퍼 호명 | `골키퍼, 김승규.` | `골키퍼, 김승규**입니다**.` | 그룹 마지막 호명(`…입니다.`)과 **같은 형태**가 되어 이름 조각이 2형태로 닫힌다 |
| `an.save.1` | `{선수}, 자리 선정이…` | `**골키퍼**, 자리 선정이…` | 해설위원 이름 클립 12 → **0** |

### 이름 조각을 왜 **2형태**로 굽는가 (`{이름},` / `{이름}입니다.`)

`입니다`를 공용 조각으로 빼면 조각 수가 147개로 반이 되지만, `이한범`+`입니다`의
이음매가 **음절 한복판**(범+입 → [버빔])에 떨어져 연음이 깨진다. 조사를 템플릿이
아니라 이름 클립에 붙인 것과 **똑같은 이유**다. 받침 없는 이름은 안 깨지지만,
이름의 절반만 자연스러운 건 품질이 아니다.

## ③ 매니페스트와 예산 — 배치 8, 한도 창 둘

`tools/tts/manifest.mjs`가 ①·②의 실측을 배치(=요청 1회)로 접는다.

**배치는 두 종류이고 절대 섞지 않는다.** 조각 수가 달라 자르기 계약이 깨진다.

| 종류 | 프롬프트 | 조각 | 쓰임 |
|---|---|---:|---|
| `plain` | 완결 문장 N개 | N | 입장 소개 문장(`네 명의 수비가 출전합니다.`) |
| `carrier` | `A …… B` 캐리어 N개 | 2N | 이름·조각. 머리=문중 억양, 꼬리=문말 하강 |

캐리어는 **문법적으로 온전한 문장**이다. 비문을 읽히면 억양이 무너진다.

```
머리 절단  {이름} …… 지금 좋습니다.        →  `{이름},`
꼬리 절단  이 선수는 …… {이름}입니다.      →  `{이름}입니다.`
머리 절단  골키퍼 …… 자리를 잡습니다.      →  `골키퍼,`
```

> **스타일 문구는 프로토가 200을 받은 것을 그대로 쓴다**(`tools/tts/proto.py`).
> "말줄임표에서 1초 이상 멈춰라"로 줄여 썼다가 HTTP 400
> `Model tried to generate text, but it should only be used for TTS`로 요청 하나를
> 태웠다. **`……`를 소리 내어 읽지 말라는 말이 빠지면** 모델이 그 자리를 "채워
> 넣으라는 지시"로 읽는다.

### 캐리어 배치 크기 = **8** (실측으로 정했다)

프로토가 16을 반증했고(음역 3.2~13.2반음, 12/16 구제 불가) 2~4는 안전하다고 했다.
5~15가 미측정이라 **8과 12를 각 1회씩 실제로 뽑아** 쟀다. 판정은 음역이 아니라
**자르기 정합성**으로 났다 — 이쪽은 추정기가 필요 없는 객관 지표다.

| 캐리어 배치 | 응답 길이 | 조각 수 (무음 문턱 0.4→1.2) | 판정 |
|---:|---:|---|---|
| 8 (C03) | 44s | **16 · 16 · 16 · 16 · 16** (기대 16) | 전 구간 정확 |
| 12 (C04-probe) | **194s** | 88 · 53 · 30 · 15 · 12 (기대 24) | **24가 나오는 문턱이 없다** |

배치 12는 음역 이전에 모델이 폭주했다(8개에 44초인데 12개에 194초). **8로 확정**.
탐침도 우선순위 앞쪽을 구웠으므로 버린 요청은 배치 12 하나뿐이다.

### 이름 클립을 절반으로 — AI 팀은 XI가 결정론이다

`{이름},`(중간 호명)과 `{이름}입니다.`(그룹 마지막·골키퍼)는 별개 클립이다.
그런데 **AI 팀은 `pickBestXI`가 XI를 고정**하므로 마지막 자리에 서는 선수가 정해져
있다. `inventory.mjs`가 런타임과 **같은 함수**(`lineupIntroBeats`)로 실제 발화
문자열을 뽑아 보니 AI 팀은 팀당 **11개**(중간 7 + 마지막·골키퍼 4)면 끝이었다 —
전원 2형태로 굽던 22개의 절반이다. 한국만 26명 × 2형태다(유저가 슬롯을 바꾼다).

### 확정 매니페스트 (scope=group, carrier 8)

| | 클립 | 요청 | 예산 20 |
|---|---:|---:|---|
| 캐스터 | 109 | **13** | 여유 7 |
| 해설위원 | 6 | **1** | 여유 19 |
| 합계 | **115** | **14** | 추정 배포 용량 **1.15 MB** (3MB 예산 안) |

**예산이 20인 이유**: 한도 창이 둘이다. 태평양 08-01(지금 ~ KST 08-02 16:00)과
08-02(KST 08-02 16:00 ~ 마감). 마감 KST 08-03 10:00 = PDT 08-02 17:00이라
두 창 다 마감 전이다.

### 오늘 창에서 구운 것 (2026-08-01 PDT)

| 배치 | 내용 | 상태 |
|---|---|---|
| C01·C02 | 입장 소개 문장 26개 전부 | ✅ |
| C03·C04 | 한국 이름 16개(김승규·이한범·이기혁·김민재·김태현·황인범·손흥민·백승호) | ✅ |
| A14 | 해설위원 문장 6개 전부 | ✅ |
| (폐기) | 배치 12 탐침, HTTP 400 1회 | 소모됨 |

**캐스터 한도 소진(429)으로 그 자리에서 멈췄다. 재시도하지 않았다.**
남은 것은 C05~C13 **9회**로, 내일 창(10회) 안에 여유 1로 들어간다.

### 재개 — 원본 wav가 체크포인트다

```
out/tts/raw/{배치}.wav      ← 있으면 그 배치는 다시 요청하지 않는다
out/tts/work/                조각 작업물(원본보다 오래되면 자동 폐기)
public/tts/{키}.mp3          배포물
public/tts/index.json        런타임 조회표 — **실제로 존재하는 mp3만** 싣는다
```

### 자르기 — 문턱을 찾지 않고 **구조**를 쓴다

문턱 하나로 맞추려 하면 배치마다 값이 다르고, 실제로 **맞는 값이 아예 없는 배치가
있었다**(C04: -40dB에서 0.4~1.2 전 구간에 16이 없음). 그래서 조각 수를 아는 것을
이용한다 — 일부러 잘게 자른 뒤 **간격이 가장 좁은 경계부터 도로 붙여** 기대 개수로
수렴시킨다. 남는 경계는 언제나 가장 크게 벌어진 자리, 즉 문장 쉼과 말줄임표 쉼이다.
다섯 배치 모두 이 방식으로 정확히 갈렸다(남은 경계 쉼 최소 0.70~2.40초).

**조각 수가 맞아도 내용은 어긋날 수 있다.** 실제로 폐기한 배치 12의 작업 캐시가
남아 8개짜리 배치 자리에 194초 오디오가 쓰였고, 조각 수는 16/16으로 "정상"이었으며
`황인범입니다.`가 **17.4초**짜리 클립으로 나왔다. 그래서 두 겹으로 막는다:
① 원본이 더 새로우면 캐시를 버린다 ② 음절 수로 클립 길이 상한을 세워 넘으면 죽는다.

### 음역 정렬 — 판정을 사용자에게 넘긴다

`f0.py`는 numpy 없는 순수 파이썬 자기상관이라 0.5초 조각에서 옥타브 배증
오검출이 난다. 이번엔 **기준 배치 자신의 산포가 6.4반음**으로 나왔다 — 그럴 리
없는 값이라 이 추정기로는 판정할 수 없다. 그래서:

- 화자별로 기준을 따로 잰다(캐스터 Puck / 해설위원 Iapetus는 다른 모델이다).
- 옥타브 접기를 넣어 배증 오검출로 정렬을 포기하지 않게 했다.
- **정렬 유무 A/B를 `docs/audio/tts-samples/`에 나란히 두었다**(`05`/`05x`, `08`/`08x`).
  정렬은 ±5반음을 넘으면 당기지 않으므로 **일부만 당겨질 수** 있고, 그게 오히려
  조각을 더 벌려 놓을 수 있다. 귀로 판정한 뒤 `--no-align`을 기본으로 삼을지 정한다.

## ④ 런타임 배선 — mp3 우선, `speechSynthesis` 폴백

`src/audio/commentary-mp3.ts` (새 모듈) + `src/audio/commentary-tts.ts` (분기 추가).
**기존 `speechSynthesis` 경로는 한 줄도 지우지 않았다.** 클립은 일부 팀만 덮으므로
폴백이 비상구가 아니라 **정상 경로**다.

- **AudioContext를 만들지 않는다.** `sfx.audioBus()`가 유저 제스처에서 연 것을 빌린다.
  그래야 음소거 토글 **하나**(`sfx.setMuted`)가 효과음·BGM·해설을 함께 끊는다.
  버스가 없으면(제스처 전·jsdom·SSR) 조용히 `false` → 폴백.
- **`HTMLAudioElement`를 쓰지 않는다.** jsdom에 반쪽만 있어 테스트가 환경에 따라 갈린다
  — `bgm.ts`가 Web Audio만 쓰는 것과 같은 이유다. fetch + `decodeAudioData` +
  `AudioBufferSourceNode`만 쓴다. 덕분에 **오디오 파일이 하나도 없어도 테스트가 돈다**
  (`src/audio/__tests__/commentary-mp3.test.ts`).
- **재생 속도**(1x/1.5x/2x)는 `playbackRate`로 간다(상한 2배).
- **순서 보존**: 대본(`speakScripted`)은 큐 꼬리 시각(`queueEndsAt`)에 이어 예약한다.
  mp3 실길이가 `estimateSpeechMs` 추정과 달라도 순서는 절대 뒤집히지 않는다.
- **전부 아니면 전무**: `beginScript(대본 전체)`가 한 줄이라도 못 덮으면 대본 전체를
  `speechSynthesis`로 낸다. 스물 몇 줄 중 몇 줄만 mp3면 한 사람이 줄마다 목소리를
  바꾸는 소리가 난다.
- **일시정지는 mp3에 걸지 않는다.** `AudioBufferSourceNode`는 멈추면 이어 재생할 수
  없고(끊으면 그 문장이 사라진다), 조각이 0.5~2초라 얼리지 않아도 어긋나지 않는다.

### 아직 남은 한 줄 — `MatchScreen.tsx` 훅

`beginScript`/`endScript`를 입장 연출에 걸어야 mp3가 실제로 울린다. `src/ui/**`는
다른 갈래 소유라 **여기서 고치지 않았다.**

```ts
// expandEntrance() 안, setEntranceScr(scr) 직후
ctts.beginScript(scr.beats.map(b => b.speech))
// handleEntranceDone() 안, ctts.stopAll() 뒤
ctts.endScript()
```

이 두 줄이 없으면 `scriptMp3`가 계속 false라 **지금과 완전히 같게 동작한다**(회귀 0).

## 도구

| 파일 | 하는 일 |
|---|---|
| `tools/tts/roster.mjs` | 캠페인 시뮬로 **이름이 불리는 선수**를 센다 → `roster.json` |
| `tools/tts/inventory.mjs` | 입장 소개가 내는 **문장 전체**를 프로덕션 코드에서 뽑는다 → `entrance-lines.json` |
| `tools/tts/manifest.mjs` | 위 둘을 배치로 접는다 → `manifest.json` (요청 수·용량 계산 포함) |
| `tools/tts/run.py` | 매니페스트대로 생성. `--dry-run` · 재개 · 한도 상한 |
| `tools/tts/assemble.py` | 자르기 → 음역 정렬 → 굽기 → `public/tts/index.json` |
| `tools/tts/register.py` | 배치 크기 ↔ 음역 흩어짐 실측(API 없음) |
| `tools/tts/sample.py` | 런타임과 같은 규칙으로 이어 붙인 청취 샘플 |
| `tools/tts/gen.py` `split.py` `join.py` `f0.py` `josa.py` | 프로토가 남긴 하부 도구 |

### 내일 아침 절차

```sh
export GEMINI_API_KEY=...          # 파일·커밋·로그에 남기지 마라
python3 tools/tts/run.py --dry-run # 마지막 확인(소모 0)
python3 tools/tts/run.py           # 남은 배치를 한도만큼 (오늘은 C05~C13)
python3 tools/tts/assemble.py      # ffmpeg만 쓴다. 실패해도 API를 다시 부르지 않는다
npx vitest run src/audio && npm run build
```
